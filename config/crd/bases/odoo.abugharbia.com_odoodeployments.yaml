---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: odoodeployments.odoo.abugharbia.com
spec:
  group: odoo.abugharbia.com
  names:
    kind: OdooDeployment
    listKind: OdooDeploymentList
    plural: odoodeployments
    singular: odoodeployment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OdooDeployment is the Schema for the odoodeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OdooDeploymentSpec defines the desired state of OdooDeployment
            properties:
              config:
                description: The configuration for the Odoo
                properties:
                  adminPasswordSecretName:
                    description: |-
                      The admin password to use for the Odoo application
                      The admin passowrd is used to create/copy or delete odoo databases
                      This can be left empty to generate a secure random password
                    type: string
                  dataDir:
                    default: /var/lib/odoo
                    description: The directory to use for the odoo filestore and session
                      store
                    type: string
                  debugMode:
                    default: false
                    description: Enable debug mode for Odoo
                    type: boolean
                  limitMemoryHard:
                    default: 2684354560
                    description: The maximum memory in bytes that the process can
                      take
                    format: int64
                    type: integer
                  limitMemorySoft:
                    default: 2147483648
                    description: The maximum memory in bytes that the process can
                      take
                    format: int64
                    type: integer
                  limitRequest:
                    default: 8192
                    description: The maximum number of requests that the process can
                      take
                    format: int32
                    type: integer
                  limitTimeCpu:
                    default: 60
                    description: The maximum CPU time in seconds that the process
                      can take
                    format: int32
                    type: integer
                  limitTimeReal:
                    default: 120
                    description: The maximum real time in seconds that the process
                      can take
                    format: int32
                    type: integer
                  proxyMode:
                    default: true
                    description: |-
                      Proxy Mode for Odoo
                      This instructs Odoo to use the X-Forwarded-For header for the remote IP address
                    type: boolean
                  withoutDemo:
                    default: true
                    description: Install modules without demo data
                    type: boolean
                  workers:
                    default: 2
                    description: Numer of process workers to use for Odoo
                    format: int32
                    type: integer
                type: object
              database:
                description: |-
                  Backup OdooBackupConfig `json:"backup,omitempty"`
                  The database configuration for the OdooDployment
                properties:
                  host:
                    default: postgresql
                    description: The database host to use for Odoo
                    type: string
                  hostFromSecret:
                    description: The database host to use for Odoo from a secret
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  maxConn:
                    default: 20
                    description: The database max connections to use for Odoo
                    format: int32
                    type: integer
                  maxConnFromSecret:
                    description: The database max connections to use for Odoo from
                      a secret
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  name:
                    default: odoo
                    description: The database name to use for Odoo
                    type: string
                  nameFromSecret:
                    description: The database name to use for Odoo from a secret
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  passwordFromSecret:
                    description: The database password to use for Odoo
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  port:
                    default: 5432
                    description: The database port to use for Odoo
                    format: int32
                    type: integer
                  portFromSecret:
                    description: The database port to use for Odoo from a secret
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  ssl:
                    default: false
                    description: Whether or not to enable SSL for the database connection
                    type: boolean
                  sslFromSecret:
                    description: Whether or not to enable SSL for the database connection
                      from a secret
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  user:
                    default: odoo
                    description: The database user to use for Odoo
                    type: string
                  userFromSecret:
                    description: The database user to use for Odoo from a secret
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - passwordFromSecret
                type: object
              image:
                default: odoo:18
                description: The image to run for the OdooDployment
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: Image pull policy for the OdooDployment
                type: string
              modules:
                default:
                - base
                description: A list of modules to initialise the database with
                items:
                  type: string
                minItems: 1
                type: array
              name:
                description: The name of the OdooDployment
                type: string
              odooFilestore:
                description: PersistentVolumeClaim defines the replicated volume specs
                properties:
                  accessModes:
                    default:
                    - ReadWriteOnce
                    description: AccessMode defines the access mode of the new persistent
                      volume claim
                    items:
                      type: string
                    type: array
                  createIfNotExists:
                    default: true
                    description: CreateIfNotExists defines if the persistent volume
                      claim should be created in case not found
                    type: boolean
                  name:
                    default: odoo-filestore
                    description: Name of the persistent volume claim
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 10Gi
                    description: StorageSize defines the size of the new persistent
                      volume claim
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    default: default
                    description: StorageClass is the storageClassName used to create
                      a new persistent volume claim
                    type: string
                type: object
              replicas:
                default: 1
                description: The number of replicas to run for the OdooDployment
                format: int32
                minimum: 1
                type: integer
            required:
            - name
            type: object
          status:
            description: OdooDeploymentStatus defines the observed state of OdooDeployment
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentInitJob:
                description: The name of the current running InitJob
                properties:
                  jobNamespace:
                    description: The name of the InitJob
                    type: string
                  modules:
                    description: The list of modules that are being installed
                    items:
                      type: string
                    type: array
                  name:
                    description: The name of the InitJob
                    type: string
                required:
                - jobNamespace
                - name
                type: object
              initModulesInstalled:
                default: []
                items:
                  type: string
                type: array
              odooAdminSecretName:
                description: The secret name for the Odoo admin password
                type: string
              odooConfigSecretName:
                default: ""
                description: The name of the Secret used to store the Odoo configuration
                  file
                type: string
              odooDataPvcName:
                default: ""
                description: The name of the PVC used for the Odoo data
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
